# RETIRED — Railway trial ended. Public hosting is Render + Vercel (see PUBLIC_DEPLOY.md).
# Kept as manual-only for emergency rollback; do not run on push.
name: Finish Railway deploy (retired)

on:
  workflow_dispatch:

jobs:
  finish:
    name: Configure Railway + wire Vercel
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: |
          echo "Railway deploy is retired. Use Render Blueprint (render.yaml) + PUBLIC_DEPLOY.md."
          exit 1
