# Infrastructure activation — 5 September 2026

This record accompanies the 2.3.0 source upgrade. A passing local test, configured environment variable, successful cloud deployment, private upload acceptance and institutional launch authority are separate evidence states.

## Completed and prepared

| Area | Evidence and result |
| --- | --- |
| Deployment credential | A time-limited Vercel token was saved directly to GitHub Actions through the authenticated local CLI. The full deployment workflow for verified commit `6cf08342f460a377c8ed00d854eb84588f11577d` succeeded on attempt 4 of [run 33939673525](https://github.com/Abdulsalam3302/IRB-Ultimate/actions/runs/33939673525). No token values are in this record. |
| Token scope | The standard Vercel CLI retrieves the owning team's settings as well as the project. A project-only token was rejected at that team lookup. The active replacement is restricted to the owning team; it therefore can access other projects in that team. Superseded temporary tokens were deleted. The workflow continues to target the exact IRB project ID. |
| Token rotation | Expires **2026-12-04**. `VERCEL_TOKEN_EXPIRES_AT` records that date in GitHub variables. New deployments warn within 14 days and stop at the rotation date. Renew through Vercel account settings and update the Actions secret and date together. The 2.3.0 workflow pins CLI 55.0.0; the successful credential-repair run used the prior CLI 52 workflow. |
| New Supabase project | `yimtuqerflrqminsujfn` responds with HTTP 200 from Auth settings and exposes an ES256 verification key. Email login is enabled, email confirmation is required, and no social OAuth providers are enabled. These public checks do not grant project administration. |
| Auth cutover configuration | The Render backend and frontend build variables point to the new tenant; `vercel.json` permits its exact origin. The platform session secret was rotated in protected configuration for the next verified deployment. Existing sessions must sign in again after activation. Passwords and application records are unchanged. |
| Login experience | Explicit applicant/institutional account selection, configured-provider discovery, confirmed-email provider login and signed server session bridge. Credentials are cleared when switching methods; failures do not silently send them to another identity system. English/Arabic mobile tests use synthetic, intercepted responses. |
| Durable objects | A private Supabase Storage adapter, bounded requests, modern server-only key handling, MIME/signature checks, overwrite refusal and short attachment URLs are implemented. Certificate backup copies use the selected remote provider. [Activation and acceptance instructions](supabase-storage-activation.md) include synthetic access-denial and cleanup checks. |
| Scanner | The pinned private ClamAV container built and ran locally. Real clean/EICAR, encrypted content, service outage, stale signatures, watchdog shutdown and recovery checks passed. See [measured local evidence](../infra/clamav/local-runtime-verification.json). No public scanner port was exposed. |
| Compressed documents | Actual scanner testing found that a 16,457-byte archive declaring a 16 MiB file could pass ClamAV. The application now checks ZIP metadata before scanning DOCX/XLSX: sizes, expansion ratio, counts, encryption, paths and structural consistency. Metadata checks and antivirus scanning remain complementary controls. |
| Supabase CLI | `supabase init` completed. Checked-in configuration is **local-only**, with a private 15 MiB bucket, confirmed email and TOTP. It is not a dump or attestation of cloud settings. No cloud SQL migration or database replacement was performed. |
| Previous storage export | The supplied `andqzedqoiduqxynoaeq.storage.zip` is a valid, empty, 22-byte ZIP: **0 entries and 0 file bytes**. There are no files to restore. It does not include a database export. |

## Pending service activation

1. **New Supabase account access.** The existing connector belongs to the previous account and cannot administer the new project. The browser sign-in did not complete; the operator was asked to sign into the new account. The supplied public key supports public Auth requests, but cannot create a bucket or provide server-side private storage. Once authorized dashboard access is available, obtain the server-only secret, inspect all storage policies, configure the private bucket, then execute the acceptance script with a dedicated synthetic user. `supabase link --project-ref yimtuqerflrqminsujfn` also requires authorized management access; no fabricated link receipt is claimed.
2. **Render scanner billing and deployment.** The proposed private service is in the API's Frankfurt region, with 2 CPU / 4 GB RAM and a 6 GB signature disk. Its incremental price checked on 5 September is **USD 86.50/month**, before tax or overages. Render Blueprint validation returned `need_payment_info`. No paid service has been created. The operator must approve the recurring cost and add billing; the [runbook](scanner-deployment.md) then covers private networking, actual engine readiness and staged probes from the API host.
3. **End-to-end upload acceptance.** After both services are ready, use synthetic accounts and a synthetic application to prove clean uploads persist and authorized downloads work, while anonymous/cross-account access, EICAR, malformed archives and scanner outage are denied. A scanner result or bucket flag alone is insufficient. `UPLOAD_SCAN_REQUIRED=true` and production durable-storage enforcement remain enabled until this is demonstrated.
4. **Institutional identity and public offer.** Establish the intended application owner by verified Supabase subject, enroll and test staff TOTP (`aal2`), validate the purchased domain/canonical origin, controller details, privacy/terms, pricing, support and bioethics-led pilot. A dashboard account is not an application Auth user. Native/previous-tenant identities are not relinked by matching email. Approval issuance and the synthetic-evaluation banner remain in their guarded state.

The existing MySQL/TiDB system remains the source of application, decision, audit, usage and session-revocation records. Changing an identity/object-storage provider does not migrate those records or confer Saudi/global institutional recognition.
